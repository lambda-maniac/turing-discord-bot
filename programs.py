fifo = { # Programs that need a file as stdin.
    # Example:
    # "program_name": ["path/to/program"]
}

stream = { # Prograsm that accept text as stdin (Usually with a flag for that).
    # Example:
    # "program_name": ["path/to/program", "--flag_that_makes_it_accept_text_as_stdin"]
}
